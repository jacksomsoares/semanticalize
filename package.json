{
  "name": "semanticalize-v2",
  "license": "MIT",
  "private": true,
  "version": "0.0.1",
  "description": "Semanticalize - compiler class app",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^2.0.0",
    "@stencil/router": "^1.0.1",
    "jquery": "^3.5.1"
  },
  "dependencies": {
    "bootstrap": "^4.5.3"
  }
}
